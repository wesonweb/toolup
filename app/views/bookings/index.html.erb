<div class="card-category"
>
 <h1><%# @tool.description %></h1>
</div>


<div class="container mb-5">
  <div class="d-flex justify-content-between my-4">
      <%# link_to " add a review", new_tool_booking_review_path(@booking), class: "btn btn-primary"%>
      <%# link_to "delete this review", _path(@booking), class: "btn btn-cancel", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this review?" } %>
  </div>
